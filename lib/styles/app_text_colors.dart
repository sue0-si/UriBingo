import 'package:flutter/material.dart';

class AppColors {
  // 메인글자색
  static const Color mainText = Color(0xff333333);

  // 메인타이틀
  static const Color mainTitle = Color(0xff999999);

  // 서브타이틀, 본문, 버튼텍스트
  static const Color subTitle = Color(0xffB3B3B3);

  // 안내, 설명
  static const Color info = Color(0xffCCCCCC);

  // 중요도 낮은 안내,설명
  static const Color info2 = Color(0xffE6E6E6);

  // 아이콘 색상
  static const Color icon = Color(0xff333333);

  // 버튼 색상
  static const Color mainButton = Color(0xFF9bc6bf);

  // 오류, 중요 알림
  static const Color caution = Color(0xFFcb7d74);
}
