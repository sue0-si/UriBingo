import 'package:flutter/material.dart';

class GroupSettingPage extends StatelessWidget {
  const GroupSettingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('그룹관리페이지')),
    );
  }
}
